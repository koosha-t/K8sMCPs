# GitHub MCP Server - Environment Configuration Template
# Copy this file to .env and fill in your values:
#   cp .env.template .env
#
# IMPORTANT: Never commit .env to version control!

# ==============================================================================
# REQUIRED CONFIGURATION
# ==============================================================================

# GitHub Personal Access Token (PAT)
# Create at: https://github.com/settings/tokens
# Required scopes:
#   - repo (full control of private repositories)
#   - read:packages (read packages)
#   - read:org (read organization data)
#   - workflow (if using GitHub Actions features)
GITHUB_TOKEN=your_github_personal_access_token_here

# ==============================================================================
# KUBERNETES CONFIGURATION
# ==============================================================================

# Kubernetes namespace for the MCP server
# Default: mcp-servers
NAMESPACE=mcp-servers

# ==============================================================================
# LOCAL DEVELOPMENT (KIND + REGISTRY)
# ==============================================================================

# Kind cluster name for local development
# Used by: make kind-up, make kind-down
# Default: mcp-dev
KIND_CLUSTER_NAME=mcp-dev

# Local Docker registry configuration
# Used by: make registry-up, make build, make push
# Note: Port 5000 is often used by macOS Control Center, so we use 5001
REGISTRY_NAME=kind-registry
REGISTRY_PORT=5001

# Docker image configuration
IMAGE_NAME=github-mcp-server
IMAGE_TAG=latest

# ==============================================================================
# GITHUB MCP SERVER CONFIGURATION
# ==============================================================================

# Toolsets to enable (comma-separated list)
# Available toolsets:
#   - actions: GitHub Actions workflows and runs
#   - code_security: Security findings and code scanning
#   - context: Repository context
#   - dependabot: Dependabot alerts and updates
#   - discussions: GitHub Discussions
#   - experiments: Experimental features
#   - gists: GitHub Gists
#   - issues: Issues management
#   - labels: Labels management
#   - notifications: Notifications
#   - orgs: Organization management
#   - projects: GitHub Projects
#   - pull_requests: Pull request management
#   - repos: Repository operations
#   - secret_protection: Secret scanning
#   - security_advisories: Security advisories
#   - stargazers: Repository stargazers
#   - users: User information
#
# Special values:
#   - "default": Enables context, repos, issues, pull_requests, users
#   - "all": Enables all available toolsets
#
# Default: default,actions,code_security
GITHUB_TOOLSETS=default,actions,code_security

# ==============================================================================
# OPTIONAL CONFIGURATION
# ==============================================================================

# Read-only mode (uncomment to enable)
# When enabled, only read operations are allowed
# GITHUB_READ_ONLY=1

# GitHub Enterprise Server host (uncomment and set for GHES)
# Example: https://github.example.com
# GITHUB_HOST=https://github.example.com

# Dynamic toolsets (beta feature, uncomment to enable)
# Enables adaptive toolset loading based on repository features
# GITHUB_DYNAMIC_TOOLSETS=1

# ==============================================================================
# NOTES
# ==============================================================================
#
# Local Development Quick Start:
# 1. cp .env.template .env
# 2. Edit .env and set GITHUB_TOKEN
# 3. make registry-up    # Start local Docker registry
# 4. make kind-up        # Create local kind cluster
# 5. make build-push     # Build and push Docker image
# 6. make deploy         # Deploy to cluster
# 7. make port-forward   # Access at localhost:8080
#
# Or use: make dev-deploy-full (runs steps 3-6 automatically)
#
# Production Deployment:
# 1. After creating .env, run: make deploy
# 2. To update configuration: make update-config
# 3. To update token: make update-secret
# 4. View logs: make logs
# 5. Access server locally: make port-forward
#
# For more information, see README.md
