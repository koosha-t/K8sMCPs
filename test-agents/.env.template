# GitHub Test Agent - Environment Configuration Template
# Copy this file to .env and fill in your values:
#   cp .env.template .env
#
# IMPORTANT: Never commit .env to version control!

# ==============================================================================
# REQUIRED CONFIGURATION
# ==============================================================================

# OpenAI API Key
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# ==============================================================================
# OPTIONAL CONFIGURATION
# ==============================================================================

# OpenAI Model to use
# Options: gpt-4o, gpt-4o-mini, gpt-4-turbo, gpt-3.5-turbo, etc.
# Default: gpt-4o
OPENAI_MODEL=gpt-4o

# GitHub MCP Server URL (SSE endpoint)
# For local development with port-forwarding: http://localhost:8080/sse
# For in-cluster access: http://github-mcp-server.mcp-servers.svc.cluster.local:8080/sse
# Default: http://localhost:8080/sse
# NOTE: The GitHub MCP server uses Supergateway - must use the /sse endpoint
GITHUB_MCP_SERVER_URL=http://localhost:8080/sse

# ==============================================================================
# USAGE NOTES
# ==============================================================================
#
# Quick Start:
# 1. cp .env.template .env
# 2. Edit .env and set your OPENAI_API_KEY
# 3. Start port-forwarding to GitHub MCP server:
#    cd ../github-mcp-server && make port-forward
# 4. In a new terminal, run the agent:
#    cd test-agents && adk web --port 9000
# 5. Open browser to http://localhost:9000
#
# Testing the Agent:
# - Try asking: "Search for popular Python repositories"
# - Try asking: "Show me recent issues in the kubernetes/kubernetes repo"
# - Try asking: "What are the latest PRs in anthropics/anthropic-sdk-python?"
# - Try asking: "Show me GitHub Actions workflow runs for repo X"
#
# Troubleshooting:
# - If agent can't connect to MCP server, ensure port-forwarding is running
# - Check that GITHUB_MCP_SERVER_URL matches your port-forward configuration
# - Verify the GitHub MCP server is deployed and running in your cluster
#
